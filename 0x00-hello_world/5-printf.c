#include<stdio.h>
/**
 *main - uses printf
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char *ptr="with proper grammar, but the outcome is a piece of art,";
      printf("%s", ptr);
      return (0);
}
